class UsuarioModel {
  String ? nombre;
  String ? apellidos;
  String ? email;
  String ? password;

  UsuarioModel({
    required this.nombre,
    required this.apellidos,
    required this.email,
    required this.password
  });
}

